# Проверка проекта по требованиям

## Последняя проверка: этапы 8–9

Сверены F-08/F-09, локальность и рекомендации §9 источника. Prompt поручений
вынесен в UTF-8 файл, правила и поддержка RU/KZ сохранены; отдельно проверены
рабочая папка и ошибки чтения. Поддержка чтения прошлых результатов prompt v1 сохранена.
34 проверки этого слоя прошли до перехода к summary.

Добавлен MeetingSummaryService через существующий LocalLLMClient: темы,
обсуждения, решения, проблемы/риски, основные поручения. У пунктов есть
источники и цитаты, у условных решений — условие. Поручения берутся по индексам
из этапа 7 и копируются без изменения сроков/milestones. Проверки схемы,
цитат и чисел не объявляются доказательством семантической правильности.

**155 тестов прошли**, включая прежние этапы; HTTP health — 200, compileall без
ошибок. Осталось прежнее предупреждение TestClient. Ollama на localhost недоступна;
реальные выводы summary и качество не проверены. Ограничения контекста,
отсутствие alignment и восстановления после аварии сохранены.

Созданы: `app/prompts/__init__.py`, `app/prompts/action_items.txt`,
`app/prompts/meeting_summary.txt`, `app/services/summarization.py`,
`app/schemas/summary.py`, `app/models/summary.py`, `app/api/summaries.py`,
`tests/unit/test_prompts.py`, `tests/unit/test_summarization.py`,
`tests/integration/test_summary_api.py`, `docs/MEETING_SUMMARY.md`.
Изменены: `app/services/extraction.py`, `app/schemas/action_item.py`,
`app/core/config.py`, `app/main.py`, `tests/integration/test_meetings.py`,
`app/services/README.md`, `README.md`, `docs/ACTION_EXTRACTION.md`,
`docs/IMPLEMENTATION_PLAN.md` и этот отчет. Предыдущие незакоммиченные изменения
этапов 5 и 7 сохранены.

## Предыдущая проверка: этап 7

Добавлено извлечение через локальную LLM с независимым интерфейсом, строгой схемой,
проверкой существующих ID и цитат, nullable ответственным/сроком, условными решениями
и milestones. Семантические правила из §9 и этапа 7 включены в prompt и acceptance
cases. Точные дубли объединяются; конфликт сроков требует нового корректного ответа.
Перефразированные дубли, принятие решения и принадлежность срока поручению определяет
модель — формат JSON и цитата сами по себе не доказывают правильную интерпретацию.

128 автотестов проверяют прежние слои и новые контракты/HTTP/SQLite. Тестовый HTTP
сервер явно возвращает заготовленный JSON; настоящий inference не подменяется.
Проверены smoke HTTP health и компиляция модулей/скриптов. Оценка настоящей LLM
остановилась с `llm_not_configured`, 9 остальных cases не выполнены. Ollama на
localhost недоступна; семантическое качество RU/KZ и всех 12 правил пока не подтверждено.

Новые файлы: `app/services/local_llm.py`, `app/services/ollama.py`,
`app/services/extraction.py`, `app/schemas/action_item.py`, `app/models/action_item.py`,
`app/api/action_items.py`, `tests/unit/test_extraction.py`, `tests/unit/test_ollama.py`,
`tests/integration/test_action_items_api.py`, `tests/fixtures/extraction_cases.json`,
`scripts/evaluate_extraction.py`, `docs/ACTION_EXTRACTION.md`.
Обновлены `app/core/config.py`, `app/main.py`, `requirements.txt`,
`tests/integration/test_meetings.py`, `app/services/README.md`, корневой README,
`docs/IMPLEMENTATION_PLAN.md` и этот отчет. Изменения этапа 5 сохранены.

Ограничения: нет alignment этапа 6; анализируется сырой STT без выдуманного speaker.
Длинные транскрипты за пределами контекста отклоняются, chunking не реализован.
Ollama нужно запустить с отключенным облаком, подготовить локальные веса и
проверить cases до заявления готовности F-05. Подробнее: [ACTION_EXTRACTION.md](ACTION_EXTRACTION.md).

## Предыдущая проверка: этап 5

**79 тестов прошли**, включая 26 новых проверок диаризации: локальные пути,
CPU/CUDA/auto, отключение телеметрии, неполные веса, пересекающаяся речь, пустой
вывод, ошибки, повторный запуск, сохранение JSON и имен после перезапуска,
валидация ручных имен, конкурентный запрос и сохранность доступа к транскрипту.
Тесты модели используют doubles, точность разделения голосов ими не измеряется.
Живой HTTP health — 200. Проверка зависимостей — без конфликтов; pyannote 4.0.7
реально импортируется, PCM16 читается в тензор с запрещенными Python socket.connect.
Телеметрия отключена. На машине нет CUDA; модель не скачана: gated-доступ HF
пока отсутствует. Полный inference и качество диаризации не заявляются.

Добавлены `app/services/diarization.py`, `app/schemas/diarization.py`,
`app/models/diarization.py`, `app/api/diarization.py`, `requirements-diarization.txt`,
`scripts/prepare_diarization.py`, `tests/unit/test_diarization.py`,
`tests/integration/test_diarization_api.py`. Обновлены config, main,
тест перечня маршрутов, README, этот отчет и план.

## Предыдущая проверка: этап 4

**53 теста прошли**: прошлые слои и новый STT, JSON-персистентность, повторное
чтение после перезапуска, ошибки весов/устройства/диска, RU/KZ/смешанный Unicode
и параметры multilingual. Тесты контрактов не считаются тестами качества.
Реальный small/CPU прогон на FLEURS и сквозная цепочка API
`upload → transcribe → transcript_raw.json → GET transcript` также выполнены.
HTTP `/health` вернул 200. Исправлено штатное завершение smoke-сервера на Windows,
чтобы освободить SQLite до очистки временной папки. Предупреждение TestClient
о переходе с httpx на httpx2 остается; ошибок в итоговых проверках нет.

Исходники STT: `app/services/transcription.py`, `app/schemas/transcript.py`,
`app/api/transcripts.py`. Добавлены `requirements-stt.txt`,
`scripts/prepare_whisper.py`, `scripts/evaluate_stt.py`,
`tests/unit/test_transcription.py`, `tests/integration/test_transcripts.py`,
`docs/STT_EVALUATION.md`. Обновлены config, lifecycle, logging, схема статусов,
README, план и smoke-скрипт. Веса, исходные записи и JSON результатов вне Git.

Источник: [ai-review/requirements.md](../ai-review/requirements.md), включая ТЗ
и оба примерных протокола. Проверены все непустые исходные файлы приложения,
тесты, конфигурация, зависимости, скрипт smoke, README, план и правила Git.
Дата: 23 сентября 2026. Это техническое ревью текущего состояния, не зачет жюри.

## Вывод

Проект соответствует архитектурному направлению ТЗ, но **не выполняет полный
обязательный MVP**. Реализованы каркас, загрузка/подготовка, адаптеры локального STT
и диаризации, ручные имена говорящих, сервис извлечения поручений. Полный inference
диаризации ожидает веса, извлечение и summary — Ollama и модель; alignment и экспорт
отсутствуют. Обнаружены ошибки KZ/RU-KZ
на модели small; см. [результаты STT](STT_EVALUATION.md).

| Требование источника | Результат проверки |
| --- | --- |
| Вход аудио/видео, §3.1 | Реализованы wav/mp3/m4a/mp4/webm, локальное хранение и WAV mono 16 kHz |
| F-01 STT | Реализован и реально проверен faster-whisper small на CPU |
| F-02 русский, F-03 казахский, F-04 смешанная речь | Реальные прогоны: RU WER 7,7%, KZ 46,7%, склеенные RU/KZ 46,3%; качество KZ/смешанной речи не закрыто |
| F-05 поручения, ответственный, срок | Реализованы сервис, JSON Schema, доказательства, nullable поля и milestones; семантика на настоящей LLM не проверена |
| F-06 диаризация и привязка к человеку | Локальный адаптер Community-1, JSON и ручные имена реализованы; нет прогона без gated-весов; поручения с текстовыми ссылками готовы, привязка к голосовым сегментам ожидает alignment |
| F-07 экспорт PDF/DOCX | Не реализовано |
| F-08 summary, F-09 транскрипт и структурированные поручения | Транскрипт в JSON; поручения и summary с источниками в SQLite; качество настоящей LLM не проверено |
| §4 запрет передачи аудио/текста внешним AI API | STT/диаризация локальные; Ollama только loopback, preflight локальных весов, без proxy/redirects; серверу нужен OLLAMA_NO_CLOUD=1 |
| §4 размещение в закрытом контуре | Каркас и преобразование запускаются локально; полный ML pipeline пока не проверен |
| §3–4 анонимизация/защита данных | Данные и секреты исключены из Git; для ML использованы открытые FLEURS, не встречи пользователя; нет авторизации, автоматической анонимизации и политик хранения |
| §2 уведомление участников | Описано в README; запись встречи и соответствующий UI еще не реализованы |
| §5–7 README и воспроизводимость | Описаны запуск, ENV, ffmpeg, STT, подготовка весов и оценка; полный lock-файл и end-to-end MVP отсутствуют |
| §3.4 дополнительные функции | Не реализованы и не начаты |
| Teams/Zoom/Meet, live-поток и напоминания | Отложены по указанию пользователя; неоднозначность обязательности сохранена в плане |
| §6–8 правила хакатона, личный вклад, присутствие и сдача | По исходному коду подтвердить нельзя; автоматическая сдача не выполнялась |

## Замечания и решения этого этапа

- Статус `created` не должен изображать готовый протокол. Добавлены отдельные
  `uploading` и `audio_ready`; последний означает только подготовленное аудио.
- Существующие записи нельзя потерять при добавлении файлов. Используется новая
  таблица `meeting_audio`, без удаления/пересоздания старой таблицы `meetings`.
- Клиентское имя не должно становиться путем. Оба локальных пути генерируются
  из UUID; формат проверяется расширением и реальным декодированием.
- Для поврежденных данных, пустой записи, отсутствия ffmpeg, таймаута и лимитов
  предусмотрены явные ошибки и удаление частичных результатов.
- Конкурирующие и повторные загрузки отклоняются атомарной сменой статуса в SQLite.
- ffmpeg не найден в системном PATH; для реальных тестов установлен локальный
  бинарник через тестовую зависимость imageio-ffmpeg. Путь запуска документирован.
- ENV-файл не добавлялся. Конфигурация работает с defaults/переменными окружения.
- Остались ограничения: синхронное преобразование, временная multipart-буферизация,
  отсутствие восстановления после аварии, авторизации и промышленного sandbox
  для декодирования недоверенных медиа. Они обозначены в README и не скрываются.
- Версии зависимостей заданы диапазонами, полного lock-файла пока нет.

## Проверки предыдущего этапа загрузки

Тесты генерируют синтетические записи: все пять форматов, включая видео MP4/WebM
с аудио. Проверяются реальный ffmpeg, mono/16000 Hz/16-bit PCM, исходный файл,
статусы, база после перезапуска, ошибки, очистка, повторная загрузка и лимиты.
Таймаут проверяется через подмену subprocess; успешная конвертация не подменяется.
Предыдущие тесты API и ENV запускаются вместе с новыми. Это не тесты точности ML.

Итог запуска: **30 passed**; реальный HTTP `/health` — **200**. Единственное
предупреждение — deprecation стороннего TestClient для httpx; ошибок нет.
